# NuclearDelete Backup (Dialog + Benchmark)

This folder stores the previous interactive NuclearDelete variant.

- `NuclearDeleteFolder.interactive.ps1`: confirmation dialog, elapsed timing, and logging behavior.
- `NuclearDeleteFolder.visible.vbs`: launcher variant that opens a visible console window.
- `NuclearDeleteFolder.menu.reg`: menu definition used with the interactive flow.

Active production files are in `NuclearDelete\` and are now minimal + silent.
