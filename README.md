# NuclearDelete

Standalone Windows context-menu permanent delete utility.

## Branches
- `main`: multi-select files+folders (mutex + Explorer selection model)
- `single`: single-target minimal delete mode
- `explorer-mode`: original interactive Explorer-style variant from backup
